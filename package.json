{
  "name": "dahara-fuel-calc",
  "version": "0.2.13",
  "private": true,
  "type": "module",
  "description": "iRacing fuel calculator overlay (Tauri + pitwall)",
  "workspaces": [
    "packages/renderer"
  ],
  "scripts": {
    "build:renderer": "npm run build -w @dahara/renderer",
    "prebuild": "node scripts/sync-version.mjs && node scripts/make-icon.mjs",
    "build": "npm run prebuild && npm run build:renderer",
    "tauri": "tauri"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.1.0",
    "typescript": "^5.6.3"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
